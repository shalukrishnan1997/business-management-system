# Django core
Django>=5.0,<6.0
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-filter>=24.0
drf-spectacular>=0.27,<1.0
django-cors-headers>=4.3,<5.0
python-decouple>=3.8

# Database (MySQL via PyMySQL — configured in Phase 3)
PyMySQL>=1.1,<2.0

# Media / files
Pillow>=10.0

# Async tasks (eager mode until Redis in Phase 17)
celery>=5.3,<6.0
redis>=5.0,<6.0

# Exports / PDFs (used in later phases)
reportlab>=4.0
openpyxl>=3.1

# Testing
pytest>=8.0
pytest-django>=4.8
factory-boy>=3.3

# Dev quality
django-extensions>=3.2
